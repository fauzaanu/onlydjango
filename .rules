##! CRITICAL_INSTRUCTIONS_BEGGINING: THIS IS THE MOST IMPORTANT INSTRUCTIONS
REQUIRED:

# Steering Protocol

```bash
# Before editing a file for the FIRST time in conversation:
uv run python get_steering.py <path> --include-defaults

# Before editing subsequent files in the same conversation:
uv run python get_steering.py <path>
```

No exceptions.

##! CRITICAL_INSTRUCTIONS_END