# Model schema

# If you are about to write any code that interacts with the django ORM make sure to double check the Model Definition and never assume the definition.
# Assuming the definition will lead to mistakes.

`uv run python manage.py model_schema "ModelName"`
